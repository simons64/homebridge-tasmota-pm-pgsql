# Platform for saving tasmota power meter devices to postgreSQL db

need to create src/dbconfig.ts from [template](dbconfig_override.ts)

```bash
# for compilation
$ npm run build
```

example config see [config.json](src/config.json)
